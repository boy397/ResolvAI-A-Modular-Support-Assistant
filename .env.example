# Not needed for the default config (params.yaml llm.provider: local) -- the local
# model runs entirely on-device with no API key. Only fill these in if you switch
# params.yaml -> llm.provider to "groq" or "gemini".
GROQ_API_KEY=
GEMINI_API_KEY=
